#!/bin/bash
set -eo pipefail

if ! grep -q 'type: python3.7' service.template; then
    echo "No line 'type: python3.7' found in the service.template file, skipping python37 buildpack."
    exit 100
fi
echo "Python 3.7 buildpack"

cat > "$2" <<TOML
[[provides]]
name = "python"

TOML

